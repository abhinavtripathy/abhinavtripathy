# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Purpose

This is the `abhinavtripathy/abhinavtripathy` GitHub special profile repository. Its `README.md` is automatically displayed on the GitHub profile page at github.com/abhinavtripathy.

## Repository Structure

The repository contains a single meaningful file:

- `README.md` — The GitHub profile page content, written in Markdown with GitHub-flavored emoji and embedded badge images.

## Working with This Repository

There are no build tools, test suites, linters, or dependencies. All changes are purely Markdown edits to `README.md`.

### Making Changes

Edit `README.md` directly. The rendered output is what appears on the GitHub profile page. GitHub renders the Markdown automatically — no local preview tooling is configured.

Badge URLs use the format `http://github-readme-streak-stats.herokuapp.com?user=abhinavtripathy&...` for the GitHub Streak stats widget.

### Committing and Pushing

```bash
git add README.md
git commit -m "Update README.md"
git push -u origin <branch-name>
```

The commit history shows this pattern has been the convention throughout the repository's history.
